# Required (prefer POSTGRES_URL from the database container)
POSTGRES_URL=postgresql://user:password@host:port/dbname

# Optional CORS
CORS_ALLOW_ORIGINS=*

# Optional auth
AUTH_ENABLED=false
JWT_SECRET=change_me_if_auth_enabled
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXP_MINUTES=60
